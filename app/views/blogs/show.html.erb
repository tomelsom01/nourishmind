<div class="showblogbackgroundcolour">
  <div class="container">
    <div class="text-center">
      <h1 class="pt-3"><%= @blog.title %></h1>
      <%= image_tag @blog.image, class: "profileimage1" if @blog.image.present? %>
    </div>
  <% @blog.content.split("\n\n").each do |paragraph| %>
    <p><%= paragraph %></p>
  <% end %>

  </div>
</div>
